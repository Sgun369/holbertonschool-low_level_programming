#include "main.h"
/**
 * mul(int a, intb) - multiplies two integer
 *
 * @a: int to multiplie
 *@b: int to multiplie
 *
 *  Return: return the result of the multiplication
 */
int mul(int a, int b)
{
	int mul = (a * b);

	return (mul);
}
